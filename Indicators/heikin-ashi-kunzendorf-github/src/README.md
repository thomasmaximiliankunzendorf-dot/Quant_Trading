# Source code notes

The uploaded `.algo` export does not contain the original C# source code. Its metadata reports `SourceIncluded: false`.

For a full source-code repository, export the indicator from cTrader with source code included, or upload the original `.cs` file from your cTrader Automate project folder.

Recommended source layout once the `.cs` file is available:

```text
src/
  ModifiedSmoothedHeikinAshi.cs
```
